---
name: Firmware Client

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  unit-tests:
    name: Client unit and regression tests
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262

      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version: "3.10"

      - name: Run focused client tests
        working-directory: tools/firmware_client
        run: PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=. python3 -B tests/test_firmware_client.py -v

      - name: Run transport and configuration regression tests
        working-directory: tools/firmware_client
        run: PYTHONDONTWRITEBYTECODE=1 PYTHONPATH=. python3 -B tests/test_firmware_client_core.py -q

      - name: Validate Python syntax and embedded packages
        working-directory: tools/firmware_client
        run: |
          python3 -m compileall -q osracer_firmware_client entry.py
          PYTHONPATH=. python3 -B -m osracer_firmware_client bundles >/dev/null

  linux-arm64-package:
    name: Standalone Linux ARM64 package
    runs-on: ubuntu-22.04-arm
    steps:
      - uses: actions/checkout@11d5960a326750d5838078e36cf38b85af677262

      - uses: actions/setup-python@5fda3b95a4ea91299a34e894583c3862153e4b97
        with:
          python-version: "3.10"

      - name: Build and smoke-test the self-contained executable
        run: tools/firmware_client/build.sh

      - name: Verify Linux ARM64 output
        run: |
          file tools/firmware_client/dist/firmware-client/osracer-firmware-client | grep -E 'ELF 64-bit.*ARM aarch64'
          tools/firmware_client/dist/firmware-client/osracer-firmware-client bundles >/dev/null
          tools/firmware_client/dist/firmware-client/osracer-firmware-client build-info >/dev/null
          tools/firmware_client/dist/firmware-client/osracer-firmware-client licenses >/dev/null
          tools/firmware_client/dist/firmware-client/osracer-firmware-client --help >/dev/null

      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a
        with:
          name: osracer-firmware-client-linux-aarch64
          path: |
            tools/firmware_client/dist/firmware-client/osracer-firmware-client
            tools/firmware_client/dist/firmware-client/osracer-firmware-client.sha256
          if-no-files-found: error
          retention-days: 7
